<% include partials/header.ejs %>

    <div class="container">
        <header class="jumbotron">
            <div class="container">
                <h1><span class="glyphicon glyphicon-cutlery"></span>欢迎来到NicePoint</h1>
                <p>你可以在这里找到全日本最正宗的中国味道！</p>
                <p>
                    <a class="btn btn-primary btn-lg" href="/restaurants/new">添加新的餐馆</a>
                </p>
            </div>
        </header>

        <div class="row text-center" style="display: flex; flex-wrap:wrap">
            <% restaurantDatas.forEach(function(restaurants){ %>
                <div class="col-md-3 col-sm-6">
                    <div class="thumbnail">
                        <img src="<%= restaurants.restaurantImage %>" alt="picture">
                        <div class="caption">
                                <h4><%= restaurants.restaurantName %></h4>
                        </div>
                        <p>
                            <a href="/restaurants/<%= restaurants._id %>" class="btn btn-primary">更多信息</a>
                        </p>
                    </div>
                </div>
            <% }); %>
        </div>

    </div>

<% include partials/footer.ejs %>